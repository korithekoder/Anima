package anima.backend;

/**
 * Class that stores and holds options that are stored in the game.
 */
class ClientPrefs {
    
    /**
     * Controls for the player.
     */
    public static var controls:Dynamic = Constants.DEFAULT_CONTROLS;
}
